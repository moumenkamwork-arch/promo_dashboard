# Production build target: the live Railway backend.
# Dev (`npm run dev`) keeps using `.env`'s relative `/api/v1` through the Vite
# proxy instead, so this file only affects `npm run build`.
VITE_API_BASE=https://promobackend-production-93d8.up.railway.app/api/v1
